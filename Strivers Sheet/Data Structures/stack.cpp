#include<bits/stdc++.h>
using namespace std;

#define MAX 10

int size=0;

//Creating a Stack

struct stack{
    int items[MAX];
    int top;
};



int main(){
    int ch;

    st *s=(st *) malloc(sizeof(st));

    createEmptyStack(s);
    
    return 0;
}